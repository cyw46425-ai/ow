{
  "name": "ow-rookie-assistant",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Hybrid RAG Overwatch rookie decision assistant",
  "scripts": {
    "build": "node build.mjs",
    "evaluate": "node scripts/evaluate-rag.mjs",
    "test": "npm run build && npm run evaluate && node scripts/test-api.mjs"
  }
}
